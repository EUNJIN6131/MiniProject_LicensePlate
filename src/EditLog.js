import * as React from "react";
import { Box } from "@mui/material";

export default function EditLog({ historyRows,
}) {
    return (
        <>
            <Box>
                {/* {historyRows[0].logId} */}
            </Box>
        </>
    );
}